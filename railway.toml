[build]
builder = "NIXPACKS"
providers = ["python"]
buildCommand = "pip install -r facebook-bot/requirements.txt --break-system-packages"

[deploy]
startCommand = "python3 -u facebook-bot/main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

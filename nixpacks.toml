# Cấu hình Nixpacks — ép Railway dùng Python thay vì tự detect Node.js
# Xem: https://nixpacks.com/docs/config

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install -r facebook-bot/requirements.txt"]

[start]
cmd = "python -u facebook-bot/main.py"
